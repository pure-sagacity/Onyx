[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
crypto = { path = "../crypto" }
config_parsing = { path = "../config_parsing" }
database = { path = "../database" }

clap = { version = "4", features = ["derive"] }
rpassword = "7.5.4"
